#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../ironhack_generator/bin/irongenerate" "$@"
  ret=$?
else 
  node  "$basedir/../ironhack_generator/bin/irongenerate" "$@"
  ret=$?
fi
exit $ret
